# Telegram Mini App: Shuffle Mindfulness

Эстетичное приложение для получения случайных посланий осознанности, созданное специально для женской аудитории (20-40 лет), интересующейся самопознанием и магическим мышлением.

## ✨ Основные функции
- **Shuffle**: Получение случайного послания одним нажатием.
- **Интерактив**: Красивая анимированная карточка с цитатой.
- **Deep Linking**: Прямой переход к посту в приватном канале.
- **Безопасность**: Валидация данных через Telegram Bot API.

## 🚀 Быстрый старт

1. **Установка**:
   ```bash
   npm install
   ```

2. **Окружение**:
   Создайте `.env` и добавьте:
   - `UPSTASH_REDIS_REST_URL`
   - `UPSTASH_REDIS_REST_TOKEN`
   - `TELEGRAM_BOT_TOKEN`

3. **Данные**:
   Заполните `data/posts.json` и выполните `npm run upload`.

4. **Тесты**:
   ```bash
   npm test
   ```

## 🛠 Техническая документация
Подробное описание архитектуры и API доступно в [docs/TECHNICAL.md](docs/TECHNICAL.md).

## ❓ FAQ
**В: Почему приложение не открывается в браузере?**
О: В режиме production API требует `initData` от Telegram. Для локальной разработки используйте `npm run dev`, который отключает проверку в dev-режиме.

**В: Как добавить новые посты?**
О: Добавьте их в `data/posts.json` и снова запустите скрипт `upload`. Redis Set автоматически добавит новые уникальные элементы.
